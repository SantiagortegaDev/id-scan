name: app
description: "Escáner de cédula colombiana — lee código de barras PDF417 y OCR"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  mobile_scanner: ^5.0.0
  google_mlkit_text_recognition: ^0.13.0
  permission_handler: ^11.0.0
  path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
